<Page …>
  <ActionBar … />
  <StackLayout class="content">
    <!-- Tes sections existantes… -->

    <!-- Nouvelle section : Liste des résultats -->
    <Label text="Résultats du test" class="session-title text-center mt-4" />
    <ListView items="{{ sessionResults }}" class="m-4">
      <ListView.itemTemplate>
        <GridLayout columns="*,*,*" rows="auto" class="p-2 border-b">
          <Label col="0" text="{{ participantId }}" />
          <Label col="1" text="{{ timeTaken + 's' }}" />
          <Label col="2" text="{{ errorCount }}" />
        </GridLayout>
      </ListView.itemTemplate>
    </ListView>
  </StackLayout>
</Page>
